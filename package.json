{
  "name": "talk",
  "version": "1.0.0",
  "description": "Talk - Local AI Chatbot",
  "scripts": {
    "dev": "concurrently \"cd backend && venv/bin/uvicorn app:app --host 0.0.0.0 --port 8000\" \"cd frontend && npm run dev\"",
    "install:all": "cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}